import { makeStyles } from "@material-ui/core";

export const styles = makeStyles(theme => ({
    paperContent: {
        padding: "50px",
        display: "flex",
        justifyContent: 'center'
    },
    qr: {
        width: '100%',
    },
    header: {
        marginBottom: "20px"
    },
    button: {
        marginTop: "15px",
        marginBottom: "30px"
    }
}))