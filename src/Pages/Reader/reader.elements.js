import { makeStyles } from "@material-ui/core";

export const styles = makeStyles(theme => ({
    paperContent: {
        padding: "100px",
        display: "flex",
        justifyContent: 'center'
    },
    qr: {
        width: '400px',
    },
    header: {
        marginBottom: "20px"
    }
}))